# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new, empty project. Update this file once the project structure and tooling are established.

## Getting Started

Add build, lint, and test commands here as the project is set up. For example:

- **Install dependencies**: (e.g., `npm install`, `pip install -r requirements.txt`)
- **Run**: (e.g., `npm start`, `python main.py`)
- **Test**: (e.g., `npm test`, `pytest`)
- **Lint**: (e.g., `npm run lint`, `ruff check .`)
